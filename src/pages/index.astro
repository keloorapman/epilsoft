---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ServiceCard from '../components/ServiceCard.astro';
import ContentSection from '../components/ContentSection.astro';
import { translations } from '../i18n';

const lang = 'fr';
const t = translations[lang];
---

<Layout
  title="Epilsoft - Épilation Laser à Bruxelles | Uccle & Etterbeek"
  description={t.metaDescription}
  lang={lang}
>
  <div class="page-wrapper start-anim">
    <Header lang={lang} />

    <main id="homepage">
      <!-- Banner avec animation d'ouverture -->
      <div class="banner">
        <div class="img">
          <div class="video-container">
            <iframe
              src="https://www.youtube.com/embed/Ye6ZwYE5CSM?autoplay=1&mute=1&loop=1&playlist=Ye6ZwYE5CSM&controls=0&showinfo=0&rel=0&modestbranding=1&playsinline=1"
              title="Présentation Epilsoft - Centre d'épilation laser"
              frameborder="0"
              allow="autoplay; encrypted-media"
              allowfullscreen
              loading="lazy"
            ></iframe>
          </div>
          <div class="video-overlay"></div>
        </div>
        <div class="logo">
          <img src="/images/logo-banner-white.svg" alt="Epilsoft" width="400" height="100" />
          <img src="/images/logo-banner-black.svg" alt="Epilsoft" class="black" width="400" height="100" />
        </div>
        <div class="banner-cta">
          <a href="/contact" class="btn-reservation" title="Prendre rendez-vous">
            Prendre rendez-vous
          </a>
        </div>
        <div class="banner-trustindex">
          <div class="trustindex-widget">
            <div class="trustindex-content">
              <div class="trustindex-stars">
                <span>★★★★★</span>
              </div>
              <div class="trustindex-rating">
                <strong>5/5</strong>
              </div>
              <div class="trustindex-text">
                Basé sur <strong>150+ avis</strong>
              </div>
              <div class="trustindex-logo">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" fill="#4285F4"/>
                  <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/>
                  <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" fill="#FBBC05"/>
                  <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/>
                </svg>
                <small>Avis Google</small>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="wrapper">
        <div class="home_bloc">
          <!-- Quote expérience -->
          <div class="quote small_cap animate fade_in">
            Plus de 5 ans d'expérience<br />
            à votre service...
          </div>

          <!-- Slider photos -->
          <div class="slider">
            <div class="item">
              <img src="/images/centres-epilation-laser-Bruxelles.jpg" alt="Centres Epilsoft Bruxelles" width="1064" height="864" fetchpriority="high" />
            </div>
          </div>

          <!-- About section -->
          <div class="about">
            <div class="bloc_shadow">
              <div class="animate fade_in">
                <h1 class="titre_main">Epilsoft</h1>
                <p class="small_cap">Centre d'épilation définitive au laser</p>
                <span class="sub_line"></span>
                <div class="about-description-home">
                  <p>Epilsoft est une chaîne de centres spécialisés en <strong>épilation définitive par une infirmière</strong> et <strong>rajeunissement laser</strong>. Présents déjà en Belgique avec 2 centres et en pleine expansion, nous nous sommes imposés comme leader des technologies laser grâce à notre expertise et à notre esprit d'innovation.</p>
                  <p>Guidés par l'excellence, la précision et la fiabilité, nous offrons à nos patients des traitements sûrs, efficaces et personnalisés.</p>
                </div>
                <span class="sub_line"></span>
                <div class="home-stats">
                  <div class="home-stat-item">
                    <div class="home-stat-number">2</div>
                    <div class="home-stat-label">Centres</div>
                  </div>
                  <div class="home-stat-item">
                    <div class="home-stat-number">5+</div>
                    <div class="home-stat-label">Ans</div>
                  </div>
                  <div class="home-stat-item">
                    <div class="home-stat-number">100%</div>
                    <div class="home-stat-label">Satisf.</div>
                  </div>
                </div>
                <span class="sub_line"></span>
                <a href="/technologie" title="Nos technologies" class="link">Nos technologies</a>
                <a href="/contact" title="Prendre rendez-vous" class="link">Prendre rendez-vous</a>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Section Pourquoi Nous Choisir -->
      <div class="wrapper section-why-choose">
        <div class="why-choose-header animate fade_in">
          <h2 class="titre_main">Pourquoi Nous Choisir ?</h2>
          <p class="small_cap">Notre expertise à votre service</p>
          <span class="sub_line"></span>
          <p class="why-intro">Nous combinons technologie de pointe et expertise certifiée pour vous offrir des résultats durables et une peau parfaitement lisse.</p>
        </div>

        <div class="why-grid">
          <div class="why-item animate fade_up">
            <div class="why-icon">
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
                <!-- Laser médical -->
                <rect x="18" y="8" width="12" height="16" rx="2" stroke="#bfa477" stroke-width="2" fill="none"/>
                <circle cx="24" cy="16" r="3" fill="#bfa477"/>
                <path d="M24 24v4" stroke="#bfa477" stroke-width="2"/>
                <path d="M20 28h8l-2 4h-4l-2-4z" stroke="#bfa477" stroke-width="2" fill="none"/>
                <!-- Rayons laser -->
                <path d="M24 32l-2 8M24 32l2 8M24 32l-6 6M24 32l6 6" stroke="#bfa477" stroke-width="1.5" stroke-linecap="round"/>
              </svg>
            </div>
            <h3 class="why-title">Technologie de Pointe</h3>
            <p class="why-description">Lasers médicaux Candela Alexandrite et Nd:Yag, sûrs et adaptés à tous les types de peau.</p>
          </div>

          <div class="why-item animate fade_up">
            <div class="why-icon">
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
                <!-- Icône Docteur/Médical -->
                <circle cx="24" cy="12" r="6" stroke="#bfa477" stroke-width="2" fill="none"/>
                <path d="M14 40c0-6 4-10 10-10s10 4 10 10" stroke="#bfa477" stroke-width="2" stroke-linecap="round"/>
                <!-- Croix médicale sur la blouse -->
                <path d="M24 32v6M21 35h6" stroke="#bfa477" stroke-width="2" stroke-linecap="round"/>
                <!-- Stéthoscope -->
                <path d="M18 24c0-3 2-5 4-5M30 24c0-3-2-5-4-5" stroke="#bfa477" stroke-width="1.5" stroke-linecap="round"/>
                <circle cx="18" cy="24" r="2" stroke="#bfa477" stroke-width="1.5" fill="none"/>
                <circle cx="30" cy="24" r="2" stroke="#bfa477" stroke-width="1.5" fill="none"/>
              </svg>
            </div>
            <h3 class="why-title">Expertise Certifiée</h3>
            <p class="why-description">Nos techniciennes laser sont formées par une infirmière pour garantir des traitements personnalisés et sécurisés.</p>
          </div>

          <div class="why-item animate fade_up">
            <div class="why-icon">
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
                <!-- Icône faisceau laser / épilation -->
                <!-- Peau avec follicules -->
                <ellipse cx="24" cy="35" rx="16" ry="8" stroke="#bfa477" stroke-width="2" fill="none"/>
                <!-- Poils éliminés -->
                <path d="M20 35v-4M24 35v-5M28 35v-4" stroke="#bfa477" stroke-width="1.5" stroke-linecap="round" stroke-dasharray="2 2" opacity="0.4"/>
                <!-- Appareil laser -->
                <rect x="18" y="8" width="12" height="12" rx="1" stroke="#bfa477" stroke-width="2" fill="none"/>
                <circle cx="24" cy="14" r="2.5" fill="#bfa477"/>
                <!-- Faisceaux laser qui descendent -->
                <path d="M20 20l-2 8M24 20v8M28 20l2 8" stroke="#bfa477" stroke-width="2" stroke-linecap="round"/>
                <path d="M18 28l-1 3M26 28v3M30 28l1 3" stroke="#bfa477" stroke-width="1" stroke-linecap="round" opacity="0.6"/>
              </svg>
            </div>
            <h3 class="why-title">Résultats Durables</h3>
            <p class="why-description">Obtenez une peau lisse et sans poils de manière permanente.</p>
          </div>
        </div>
      </div>

      <!-- Bandeau mood -->
      <div class="headband mood">
        <div class="wrapper animate fade_in">
          <span><strong>Expertise et technologie de pointe...</strong></span>
          <strong>Tout est mis en œuvre pour des résultats définitifs et durables</strong>
        </div>
      </div>

      <!-- Section Technologies Laser -->
      <div class="wrapper section-technology">
        <div class="technology-header animate fade_in">
          <h2 class="titre_main">Technologie Médicale de Pointe</h2>
          <p class="small_cap">Nos Lasers</p>
          <span class="sub_line"></span>
        </div>

        <div class="technology-carousel">
          <div class="technology-carousel-container">
            <div class="technology-grid">
              <!-- Laser Candela -->
              <div class="tech-item animate fade_up">
                <div class="tech-image">
                  <img src="/images/laser-Candela-epilation-laser.jpg" alt="Laser Candela Gentlease" width="284" height="300" />
                </div>
                <div class="tech-content">
                  <h3 class="tech-title">Laser Candela</h3>
                  <h4 class="tech-subtitle">Le Laser Candela Gentlease</h4>
                  <p class="tech-description">Combine les longueurs d'onde Alexandrite et Nd:Yag pour traiter efficacement tous les types de peau, des plus clairs aux plus foncés, avec un confort optimal.</p>
                </div>
              </div>

              <!-- Laser Alexandrite -->
              <div class="tech-item animate fade_up">
                <div class="tech-image">
                  <img src="/images/laser-diode.jpg" alt="Laser Alexandrite" width="800" height="600" />
                </div>
                <div class="tech-content">
                  <h3 class="tech-title">Laser Alexandrite</h3>
                  <h4 class="tech-subtitle">Le Laser Alexandrite</h4>
                  <p class="tech-description">Idéal pour les peaux claires (phototypes 1 à 3), il cible la mélanine du poil avec une très grande précision pour des résultats rapides et une efficacité maximale.</p>
                </div>
              </div>

              <!-- Laser Nd Yag -->
              <div class="tech-item animate fade_up">
                <div class="tech-image">
                  <img src="/images/laser-ndyag.jpg" alt="Laser Nd Yag" width="800" height="600" />
                </div>
                <div class="tech-content">
                  <h3 class="tech-title">Laser Nd Yag</h3>
                  <h4 class="tech-subtitle">Le Laser Nd Yag</h4>
                  <p class="tech-description">Parfaitement sûr pour les peaux mates, foncées et noires (phototypes 4 à 6). Il pénètre en profondeur pour traiter le poil sans risque pour la peau.</p>
                </div>
              </div>
            </div>
          </div>

          <!-- Navigation carrousel (mobile/tablet only) -->
          <button class="tech-carousel-btn tech-prev-btn" aria-label="Précédent">‹</button>
          <button class="tech-carousel-btn tech-next-btn" aria-label="Suivant">›</button>

          <!-- Indicateurs (mobile/tablet only) -->
          <div class="tech-carousel-indicators">
            <button class="tech-indicator active" data-index="0" aria-label="Afficher laser Candela"></button>
            <button class="tech-indicator" data-index="1" aria-label="Afficher laser Alexandrite"></button>
            <button class="tech-indicator" data-index="2" aria-label="Afficher laser Nd Yag"></button>
          </div>
        </div>

        <div class="technology-cta animate fade_in">
          <a href="/technologie" title="Découvrir nos technologies" class="link">En savoir plus sur nos technologies</a>
        </div>
      </div>

      <!-- Section Traitement -->
      <div class="wrapper section-treatment">
        <div class="photo_texte coloration">
          <div class="photo">
            <img src="/images/epilation-laser-visage-avant-apres-.jpg" alt="Épilation laser visage avant après" width="742" height="1112" />
          </div>
          <div class="bloc_shadow">
            <div class="animate fade_in">
              <h3 class="titre_main">Traitement</h3>
              <span class="sub_line"></span>
              <p>Un protocole en 4 étapes pour garantir votre confort et des résultats optimaux. Consultation gratuite, séances personnalisées et suivi post-traitement inclus.</p>
              <span class="sub_line"></span>
              <a href="/traitement" title="En savoir plus" class="link">En savoir plus</a>
            </div>
          </div>
        </div>
      </div>

      <!-- Section Avis Clients -->
      <div class="wrapper section-testimonials">
        <div class="testimonials-header animate fade_in">
          <h2 class="titre_main">Ce que disent nos clients</h2>
          <p class="testimonials-subtitle">Découvrez les expériences de ceux qui ont fait confiance à Epilsoft pour leur parcours d'épilation laser</p>
          <span class="sub_line"></span>
        </div>

        <div class="testimonials-carousel">
          <div class="carousel-container">
            <div class="carousel-track">
              <!-- Avis 1 - Sarah M. -->
              <div class="testimonial-card active">
                <div class="testimonial-badge">Plus Populaire</div>
                <div class="testimonial-header-with-photo">
                  <div class="testimonial-photo">
                    <img src="/images/testimonial-1.svg" alt="Sarah M." />
                  </div>
                  <div class="testimonial-info">
                    <div class="testimonial-stars">★★★★★</div>
                    <div class="author-name">Sarah M.</div>
                    <div class="author-location">Brussels</div>
                  </div>
                </div>
                <p class="testimonial-text">"Excellent service! The staff is very professional and welcoming. The results are visible from the first sessions. I highly recommend Epilsoft for anyone looking for effective and lasting laser hair removal."</p>
              </div>

              <!-- Avis 2 - Julie D. -->
              <div class="testimonial-card">
                <div class="testimonial-header-with-photo">
                  <div class="testimonial-photo">
                    <img src="/images/testimonial-2.svg" alt="Julie D." />
                  </div>
                  <div class="testimonial-info">
                    <div class="testimonial-stars">★★★★★</div>
                    <div class="author-name">Julie D.</div>
                    <div class="author-location">Uccle</div>
                  </div>
                </div>
                <p class="testimonial-text">"Je vais chez Epilsoft depuis plus d'un an et je suis absolument ravie ! Les traitements sont indolores et les résultats ont dépassé mes attentes. L'équipe est attentive et prend toujours le temps de répondre à toutes mes questions."</p>
              </div>

              <!-- Avis 3 - Marc L. -->
              <div class="testimonial-card">
                <div class="testimonial-header-with-photo">
                  <div class="testimonial-photo">
                    <img src="/images/testimonial-3.svg" alt="Marc L." />
                  </div>
                  <div class="testimonial-info">
                    <div class="testimonial-stars">★★★★★</div>
                    <div class="author-name">Marc L.</div>
                    <div class="author-location">Etterbeek</div>
                  </div>
                </div>
                <p class="testimonial-text">"Résultats exceptionnels ! Après seulement quelques séances, ma peau est devenue parfaitement lisse. Le personnel est très compétent et les installations sont modernes. Je recommande vivement Epilsoft à tous ceux qui recherchent une solution d'épilation définitive."</p>
              </div>

              <!-- Avis 4 - Sophie V. -->
              <div class="testimonial-card">
                <div class="testimonial-header-with-photo">
                  <div class="testimonial-photo">
                    <img src="/images/testimonial-4.svg" alt="Sophie V." />
                  </div>
                  <div class="testimonial-info">
                    <div class="testimonial-stars">★★★★★</div>
                    <div class="author-name">Sophie V.</div>
                    <div class="author-location">Ixelles</div>
                  </div>
                </div>
                <p class="testimonial-text">"Une expérience formidable du début à la fin. L'équipe d'Epilsoft est professionnelle, à l'écoute et très rassurante. Les traitements sont rapides et efficaces. Je ne peux que recommander leurs services !"</p>
              </div>

              <!-- Avis 5 - Thomas B. -->
              <div class="testimonial-card">
                <div class="testimonial-header-with-photo">
                  <div class="testimonial-photo">
                    <img src="/images/testimonial-5.svg" alt="Thomas B." />
                  </div>
                  <div class="testimonial-info">
                    <div class="testimonial-stars">★★★★★</div>
                    <div class="author-name">Thomas B.</div>
                    <div class="author-location">Woluwe</div>
                  </div>
                </div>
                <p class="testimonial-text">"Excellent rapport qualité-prix. Les technologies utilisées sont à la pointe et le professionnalisme de l'équipe est remarquable. Je suis très satisfait des résultats obtenus après mon traitement chez Epilsoft."</p>
              </div>

              <!-- Avis 6 - Emma R. -->
              <div class="testimonial-card">
                <div class="testimonial-header-with-photo">
                  <div class="testimonial-photo">
                    <img src="/images/testimonial-6.svg" alt="Emma R." />
                  </div>
                  <div class="testimonial-info">
                    <div class="testimonial-stars">★★★★★</div>
                    <div class="author-name">Emma R.</div>
                    <div class="author-location">Uccle</div>
                  </div>
                </div>
                <p class="testimonial-text">"Je recommande sans hésitation ! L'accueil est chaleureux, les conseils personnalisés et les résultats sont au rendez-vous. Epilsoft a vraiment changé ma vie avec des traitements efficaces et confortables."</p>
              </div>
            </div>
          </div>

          <!-- Navigation du carrousel -->
          <button class="carousel-btn prev-btn" aria-label="Précédent">‹</button>
          <button class="carousel-btn next-btn" aria-label="Suivant">›</button>

          <!-- Indicateurs -->
          <div class="carousel-indicators">
            <button class="indicator active" data-index="0" aria-label="Afficher témoignage 1"></button>
            <button class="indicator" data-index="1" aria-label="Afficher témoignage 2"></button>
            <button class="indicator" data-index="2" aria-label="Afficher témoignage 3"></button>
            <button class="indicator" data-index="3" aria-label="Afficher témoignage 4"></button>
            <button class="indicator" data-index="4" aria-label="Afficher témoignage 5"></button>
            <button class="indicator" data-index="5" aria-label="Afficher témoignage 6"></button>
          </div>
        </div>
      </div>

      <div class="wrapper">
        <div class="animate fade_in">
          <a href="/contact" class="link_eshop" title="Prendre rendez-vous">
            <span>Prendre rendez-vous dès maintenant</span>
          </a>
        </div>
      </div>
    </main>

    <!-- Bandeau final -->
    <div class="headband">
      <div class="wrapper animate fade_in">
        <span><strong>Un accueil personnalisé et professionnel,</strong></span>
        <strong>des technologies laser de dernière génération pour des résultats définitifs</strong>
      </div>
    </div>

    <Footer lang={lang} />

    <!-- Popup Promo -->
    <div class="promo-popup-overlay" id="promo-popup">
      <div class="promo-popup">
        <button class="popup-close" id="popup-close" aria-label="Fermer">&times;</button>
        <div class="popup-badge">🎁 OFFRE EXCLUSIVE</div>
        <h2 class="popup-title">-50% sur vos 8 séances laser</h2>
        <p class="popup-subtitle">Profitez de notre offre exceptionnelle pour une peau parfaitement lisse</p>
        <div class="popup-highlight">
          <div class="highlight-item">
            <div class="highlight-number">50%</div>
            <div class="highlight-label">de réduction</div>
          </div>
          <div class="highlight-item">
            <div class="highlight-number">8</div>
            <div class="highlight-label">séances laser</div>
          </div>
        </div>
        <p class="popup-description">
          Offre valable pour toute nouvelle réservation. Traitement personnalisé avec nos lasers médicaux de dernière génération.
        </p>
        <a href="/contact" class="popup-cta">Réserver maintenant</a>
        <p class="popup-terms">*Offre limitée dans le temps. Voir conditions en centre.</p>
      </div>
    </div>
  </div>
</Layout>

<style>
  /* Animation d'ouverture Semeraro Style */
  .start-anim :global(.header) {
    animation: header 3s ease;
  }

  .start-anim .banner .img:before {
    animation: banner_img 3s ease;
    width: 100%;
    height: 100%;
    position: absolute;
    left: 0;
    bottom: 0;
    background: #fff;
    content: "";
    transform-origin: bottom;
  }

  .start-anim .banner .logo {
    animation: banner_logo 3s ease;
  }

  .start-anim .banner .logo .black {
    animation: banner_logo_black 3s ease;
  }

  .start-anim .home_bloc {
    animation: home_bloc 3s ease;
  }

  @keyframes banner_img {
    0% { opacity: 1; transform: scaleY(1); }
    60% { opacity: 1; transform: scaleY(1); }
    80% { opacity: 0; transform: scaleY(0); }
    100% { opacity: 0; transform: scaleY(0); }
  }

  @keyframes banner_logo {
    0% { transform: translateY(0); }
    88% { transform: translateY(0); }
    100% { transform: translateY(40px); }
  }

  @keyframes banner_logo_black {
    0% { opacity: 0; }
    15% { opacity: 0; }
    55% { opacity: 1; }
    60% { opacity: 1; }
    66% { opacity: 1; }
    70% { opacity: 0; }
  }

  @keyframes header {
    0% { transform: translateY(-100%); }
    88% { transform: translateY(-100%); }
    100% { transform: translateY(0); }
  }

  @keyframes home_bloc {
    0% { opacity: 0; }
    80% { opacity: 0; }
    100% { opacity: 1; }
  }

  /* Banner Hero */
  .banner {
    width: 100%;
    height: 100vh;
    min-height: 600px;
    margin: 0 auto;
    position: relative;
    background: #0c0c0c;
    overflow: hidden;
    z-index: 20;
  }

  .banner .img {
    width: 100%;
    height: 100%;
    position: absolute;
    left: 0;
    top: 0;
    background: linear-gradient(135deg, #bfa477 0%, #a88d62 100%);
    overflow: hidden;
  }

  .banner .video-container {
    width: 100%;
    height: 100%;
    position: absolute;
    left: 0;
    top: 0;
    overflow: hidden;
    opacity: 1;
  }

  .start-anim .banner .video-container {
    animation: video_reveal 3s ease forwards;
    opacity: 1;
  }

  @keyframes video_reveal {
    0% { opacity: 0; }
    60% { opacity: 0; }
    80% { opacity: 1; }
    100% { opacity: 1; }
  }

  .banner .video-container iframe {
    width: 100vw;
    height: 56.25vw; /* 16:9 aspect ratio */
    min-height: 100vh;
    min-width: 177.77vh; /* 16:9 aspect ratio */
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
  }

  .banner .video-overlay {
    width: 100%;
    height: 100%;
    position: absolute;
    left: 0;
    top: 0;
    background: rgba(0, 0, 0, 0.3);
    z-index: 1;
    opacity: 1;
  }

  .start-anim .banner .video-overlay {
    animation: video_reveal 3s ease forwards;
    opacity: 1;
  }

  .banner .logo {
    width: 400px;
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, calc(-50% + 40px));
    z-index: 10;
  }

  .banner .logo img {
    width: 100%;
    height: auto;
  }

  .banner .logo .black {
    position: absolute;
    left: 0;
    top: 0;
    opacity: 0;
  }

  .banner-cta {
    position: absolute;
    bottom: 50px;
    left: 5vw;
    z-index: 20;
  }

  .btn-reservation {
    display: inline-block;
    padding: 0 40px;
    height: 56px;
    line-height: 56px;
    font-family: "Barlow", sans-serif;
    font-size: 14px;
    font-weight: 600;
    text-align: center;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: #fff;
    background: #8a7044;
    border: 2px solid #8a7044;
    cursor: pointer;
    transition: all 400ms ease-in-out;
  }

  .btn-reservation:hover {
    background: #181818;
    color: #fff;
    border-color: #181818;
  }

  .start-anim .banner-cta {
    animation: banner_cta 3s ease;
  }

  @keyframes banner_cta {
    0% { opacity: 0; transform: translateX(-30px); }
    88% { opacity: 0; transform: translateX(-30px); }
    100% { opacity: 1; transform: translateX(0); }
  }

  .banner-trustindex {
    position: absolute;
    bottom: 50px;
    right: 60px;
    z-index: 20;
    max-width: 300px;
  }

  .start-anim .banner-trustindex {
    animation: banner_trustindex 3s ease;
  }

  @keyframes banner_trustindex {
    0% { opacity: 0; transform: translateX(30px); }
    88% { opacity: 0; transform: translateX(30px); }
    100% { opacity: 1; transform: translateX(0); }
  }

  .trustindex-widget {
    background: rgba(255, 255, 255, 0.95);
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
  }

  .trustindex-content {
    text-align: center;
  }

  .trustindex-stars {
    font-size: 24px;
    color: #ffa500;
    margin-bottom: 8px;
    letter-spacing: 2px;
  }

  .trustindex-rating {
    font-size: 28px;
    font-weight: 700;
    color: #222;
    margin-bottom: 5px;
  }

  .trustindex-text {
    font-size: 14px;
    color: #666;
    margin-bottom: 8px;
  }

  .trustindex-text strong {
    color: #222;
    font-weight: 600;
  }

  .trustindex-logo {
    font-size: 12px;
    color: #999;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 5px;
  }

  .trustindex-logo svg {
    display: inline-block;
    vertical-align: middle;
  }

  /* Section Pourquoi Nous Choisir */
  .section-why-choose {
    padding: 100px 0;
    text-align: center;
  }

  .why-choose-header {
    margin-bottom: 60px;
  }

  .why-choose-header .titre_main {
    margin-bottom: 10px;
  }

  .why-choose-header .small_cap {
    margin-bottom: 30px;
  }

  .why-intro {
    max-width: 700px;
    margin: 0 auto;
    font-size: 16px;
    line-height: 28px;
    color: #666;
  }

  .why-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 60px;
    margin-top: 60px;
  }

  .why-item {
    text-align: center;
    padding: 40px 30px;
    background: #fff;
    border-radius: 8px;
    transition: all 400ms ease-in-out;
  }

  .why-item:hover {
    box-shadow: 0 8px 30px rgba(191, 164, 119, 0.15);
    transform: translateY(-5px);
  }

  .why-icon {
    margin-bottom: 25px;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .why-icon svg {
    transition: transform 400ms ease-in-out;
  }

  .why-item:hover .why-icon svg {
    transform: scale(1.1);
  }

  .why-title {
    font-family: "PT Serif", serif;
    font-size: 22px;
    font-weight: 700;
    color: #bfa477;
    margin-bottom: 15px;
    letter-spacing: 0.3px;
  }

  .why-description {
    font-size: 15px;
    line-height: 26px;
    color: #666;
  }

  /* Section Testimonials */
  .section-testimonials {
    padding: 100px 0;
    background: #f9f9f9;
  }

  .testimonials-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .testimonials-header .titre_main {
    margin-bottom: 15px;
  }

  .testimonials-subtitle {
    max-width: 700px;
    margin: 0 auto 30px;
    font-size: 16px;
    line-height: 28px;
    color: #666;
  }

  .testimonials-carousel {
    position: relative;
    max-width: 800px;
    margin: 0 auto;
  }

  .carousel-container {
    overflow: hidden;
    padding: 10px 0;
  }

  .carousel-track {
    display: flex;
    transition: transform 500ms ease-in-out;
  }

  .testimonial-card {
    min-width: 100%;
    background: #fff;
    padding: 50px;
    border-radius: 12px;
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.08);
    position: relative;
    opacity: 0;
    transition: opacity 400ms ease-in-out;
  }

  .testimonial-card.active {
    opacity: 1;
  }

  .testimonial-badge {
    position: absolute;
    top: 20px;
    right: 20px;
    background: #bfa477;
    color: #fff;
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .testimonial-header-with-photo {
    display: flex;
    align-items: center;
    gap: 20px;
    margin-bottom: 25px;
  }

  .testimonial-photo {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    overflow: hidden;
    flex-shrink: 0;
    border: 3px solid #bfa477;
  }

  .testimonial-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .testimonial-info {
    flex: 1;
  }

  .testimonial-stars {
    font-size: 20px;
    color: #ffa500;
    margin-bottom: 8px;
    letter-spacing: 2px;
  }

  .testimonial-text {
    font-size: 16px;
    line-height: 30px;
    color: #444;
    font-style: italic;
    min-height: 100px;
  }

  .author-name {
    font-family: "PT Serif", serif;
    font-size: 18px;
    font-weight: 700;
    color: #8a7044;
    margin-bottom: 3px;
  }

  .author-location {
    font-size: 13px;
    color: #757575;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .carousel-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 50px;
    height: 50px;
    background: rgba(191, 164, 119, 0.9);
    border: none;
    border-radius: 50%;
    color: #fff;
    font-size: 28px;
    cursor: pointer;
    transition: all 300ms ease;
    z-index: 10;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
  }

  .carousel-btn:hover {
    background: #bfa477;
    transform: translateY(-50%) scale(1.1);
  }

  .prev-btn {
    left: -70px;
  }

  .next-btn {
    right: -70px;
  }

  .carousel-indicators {
    display: flex;
    justify-content: center;
    gap: 10px;
    margin-top: 30px;
  }

  .indicator {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    border: none;
    background: transparent;
    cursor: pointer;
    transition: all 300ms ease;
    padding: 0;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .indicator::after {
    content: '';
    width: 10px;
    height: 10px;
    border-radius: 50%;
    border: 2px solid #bfa477;
    background: transparent;
    transition: all 300ms ease;
  }

  .indicator.active::after {
    background: #bfa477;
    transform: scale(1.3);
  }

  /* Section Technology */
  .section-technology {
    padding: 100px 0;
  }

  .technology-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .technology-header .titre_main {
    margin-bottom: 10px;
  }

  .technology-header .small_cap {
    margin-bottom: 30px;
  }

  .technology-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
    margin-bottom: 60px;
  }

  .tech-item {
    background: #fff;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    transition: all 400ms ease-in-out;
  }

  .tech-item:hover {
    transform: translateY(-8px);
    box-shadow: 0 8px 30px rgba(191, 164, 119, 0.2);
  }

  .tech-image {
    width: 100%;
    height: 350px;
    overflow: hidden;
    background: #f8f8f8;
  }

  .tech-image img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center;
    transition: transform 400ms ease-in-out;
    padding: 20px;
  }

  .tech-item:hover .tech-image img {
    transform: scale(1.05);
  }

  .tech-content {
    padding: 30px;
  }

  .tech-title {
    font-family: "PT Serif", serif;
    font-size: 24px;
    font-weight: 700;
    color: #bfa477;
    margin-bottom: 10px;
    letter-spacing: 0.3px;
  }

  .tech-subtitle {
    font-family: "PT Serif", serif;
    font-size: 16px;
    font-weight: 400;
    font-style: italic;
    color: #666;
    margin-bottom: 15px;
  }

  .tech-description {
    font-size: 15px;
    line-height: 26px;
    color: #444;
  }

  .technology-cta {
    text-align: center;
    margin-top: 40px;
  }

  .technology-cta .link {
    width: auto;
    padding: 0 50px;
  }

  /* Technology Carousel (mobile/tablet only) */
  .technology-carousel {
    position: relative;
  }

  .technology-carousel-container {
    overflow: visible;
  }

  .tech-carousel-btn {
    display: none;
  }

  .tech-carousel-indicators {
    display: none;
  }

  @media (max-width: 1200px) {
    .technology-carousel {
      position: relative;
      padding-bottom: 8px;
    }

    .technology-carousel-container {
      overflow: hidden;
      margin: 0;
      padding: 0;
    }

    .technology-grid {
      display: flex;
      transition: transform 400ms ease-in-out;
      gap: 0;
      margin-bottom: 0;
      width: 100%;
    }

    .tech-item {
      min-width: 100%;
      max-width: 100%;
      width: 100%;
      flex-shrink: 0;
      flex-grow: 0;
      background: transparent;
      box-shadow: none;
      border-radius: 0;
      padding: 0;
      margin: 0;
      box-sizing: border-box;
    }

    .tech-item:hover {
      transform: none;
      box-shadow: none;
    }

    .tech-image {
      height: 200px;
      border-radius: 0;
      width: 100%;
      background: #f8f8f8;
    }

    .tech-image img {
      object-position: center;
      width: 100%;
      height: 100%;
      object-fit: contain;
      padding: 15px;
    }

    .tech-item:hover .tech-image img {
      transform: none;
    }

    .tech-content {
      padding: 6px 5vw 2px 5vw;
      background: #fff;
      text-align: center;
    }

    .tech-title {
      font-size: 18px;
      margin-bottom: 3px;
      line-height: 1.3;
    }

    .tech-subtitle {
      font-size: 13px;
      margin-bottom: 4px;
      line-height: 1.4;
    }

    .tech-description {
      font-size: 12px;
      line-height: 18px;
      color: #444;
      max-width: 100%;
    }

    .tech-carousel-btn {
      display: block;
      position: absolute;
      top: 70px;
      transform: translateY(-50%);
      width: 45px;
      height: 45px;
      background: rgba(191, 164, 119, 0.9);
      border: none;
      border-radius: 50%;
      font-size: 28px;
      color: #fff;
      cursor: pointer;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      transition: all 300ms ease;
      z-index: 10;
      line-height: 1;
    }

    .tech-carousel-btn:hover {
      background: #bfa477;
      transform: translateY(-50%) scale(1.1);
    }

    .tech-prev-btn {
      left: 15px;
    }

    .tech-next-btn {
      right: 15px;
    }

    .tech-carousel-indicators {
      display: flex;
      justify-content: center;
      gap: 10px;
      margin-top: 8px;
      position: absolute;
      bottom: 0px;
      left: 50%;
      transform: translateX(-50%);
    }

    .tech-indicator {
      width: 44px;
      height: 44px;
      border-radius: 50%;
      border: none;
      background: transparent;
      cursor: pointer;
      transition: all 300ms ease;
      padding: 0;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .tech-indicator::after {
      content: '';
      width: 10px;
      height: 10px;
      border-radius: 50%;
      border: 2px solid #bfa477;
      background: transparent;
      transition: all 300ms ease;
    }

    .tech-indicator.active::after {
      background: #bfa477;
      transform: scale(1.3);
    }

    .tech-indicator:hover::after {
      transform: scale(1.2);
    }
  }

  /* Home Bloc Structure Semeraro */
  .home_bloc {
    position: relative;
    z-index: 40;
    padding: 0 0 140px 50%;
    margin-top: -50px;
  }

  .home_bloc:before {
    width: 200%;
    height: 100%;
    position: absolute;
    left: 50%;
    top: 0;
    background: #f4f4f4;
    content: "";
    z-index: 1;
  }

  .home_bloc .quote {
    position: absolute;
    left: 0;
    top: 60px;
    padding-top: 50px;
    z-index: 5;
  }

  .home_bloc .about {
    position: relative;
    z-index: 20;
  }

  .home_bloc .bloc_shadow {
    margin-bottom: -100px;
    transition: transform 400ms ease-out;
  }

  .home_bloc .bloc_shadow .link {
    width: 320px;
  }

  .about-description-home p {
    font-size: 15px;
    line-height: 26px;
    margin-bottom: 15px;
    text-align: center;
  }

  .about-description-home strong {
    color: #bfa477;
    font-weight: 600;
  }

  .home-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin: 30px 0;
  }

  .home-stat-item {
    text-align: center;
  }

  .home-stat-number {
    font-family: "PT Serif", serif;
    font-size: 32px;
    font-weight: 700;
    color: #bfa477;
    line-height: 1;
    margin-bottom: 8px;
  }

  .home-stat-label {
    font-size: 12px;
    color: #666;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .home_bloc .slider {
    width: 500px;
    height: 100%;
    position: absolute;
    left: 0;
    bottom: 0;
    z-index: 25;
    padding: 180px 0 140px 0;
    display: block;
  }

  .home_bloc .slider .item {
    width: 100%;
    height: 100%;
  }

  .home_bloc .slider img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  :global(body.sticky) .home_bloc .bloc_shadow {
    transform: translateY(-100px);
  }

  /* Responsive */
  @media (max-width: 1200px) {
    .banner {
      min-height: 600px;
    }

    .banner .video-container iframe {
      width: 177.78vh;
      height: 100vh;
      min-height: 100%;
      min-width: 100%;
      left: 60%;
    }

    .section-why-choose {
      padding: 80px 0;
    }

    .why-grid {
      gap: 40px;
    }

    .why-item {
      padding: 35px 25px;
    }

    .section-testimonials {
      padding: 80px 0;
    }

    .testimonials-header {
      margin-bottom: 50px;
    }

    .testimonial-card {
      padding: 40px;
    }

    .testimonial-text {
      font-size: 15px;
      line-height: 28px;
    }

    .prev-btn {
      left: -60px;
    }

    .next-btn {
      right: -60px;
    }

    .section-technology {
      padding: 80px 0;
    }

    .technology-header {
      margin-bottom: 50px;
    }

    .technology-grid {
      gap: 30px;
      margin-bottom: 50px;
    }

    .tech-image {
      height: 220px;
    }

    .tech-content {
      padding: 25px;
    }

    .tech-title {
      font-size: 22px;
    }

    .tech-subtitle {
      font-size: 15px;
    }

    .home_bloc {
      padding: 0 0 100px 0;
      margin-top: 0;
    }

    .home_bloc:before {
      display: none;
    }

    .home_bloc .quote {
      display: none;
    }

    .home_bloc .bloc_shadow {
      width: 100%;
      margin-left: 0;
      transform: translateY(-40px);
    }

    .home_bloc .slider {
      width: 100%;
      position: relative;
      padding: 0;
      margin-bottom: 30px;
      display: block;
    }

    .home_bloc .slider .item {
      width: 100%;
      height: 350px;
    }
  }

  @media (max-width: 800px) {
    .banner {
      height: 80vh;
      min-height: 500px;
    }

    .banner .video-container iframe {
      width: 177.78vh;
      height: 100vh;
      min-height: 100%;
      min-width: 100%;
      left: 60%;
    }

    .banner .logo {
      width: 320px;
      margin-top: 30px;
    }

    .banner-cta {
      bottom: 30px;
      left: 5vw;
    }

    .section-why-choose {
      padding: 60px 0;
    }

    .why-choose-header {
      margin-bottom: 40px;
    }

    .why-grid {
      grid-template-columns: 1fr;
      gap: 30px;
      margin-top: 40px;
    }

    .why-item {
      padding: 30px 25px;
    }

    .why-title {
      font-size: 20px;
    }

    .why-description {
      font-size: 14px;
      line-height: 24px;
    }

    .section-testimonials {
      padding: 60px 0 40px 0;
    }

    .testimonials-header {
      margin-bottom: 30px;
    }

    .testimonial-card {
      padding: 30px 20px;
      min-height: auto;
    }

    .carousel-indicators {
      margin-top: 20px;
    }

    .testimonial-header-with-photo {
      gap: 15px;
      margin-bottom: 20px;
    }

    .testimonial-photo {
      width: 70px;
      height: 70px;
    }

    .testimonial-stars {
      font-size: 18px;
    }

    .testimonial-text {
      font-size: 14px;
      line-height: 26px;
      min-height: auto;
    }

    .author-name {
      font-size: 16px;
    }

    .author-location {
      font-size: 12px;
    }

    .prev-btn {
      left: 10px;
    }

    .next-btn {
      right: 10px;
    }

    .carousel-btn {
      width: 45px;
      height: 45px;
      font-size: 24px;
    }

    .section-technology {
      padding: 60px 0;
    }

    .technology-header {
      margin-bottom: 40px;
    }

    .technology-grid {
      grid-template-columns: 1fr;
      gap: 30px;
      margin-bottom: 40px;
    }

    .tech-image {
      height: 200px;
    }

    .tech-content {
      padding: 25px;
    }

    .tech-title {
      font-size: 20px;
    }

    .tech-subtitle {
      font-size: 14px;
    }

    .tech-description {
      font-size: 14px;
      line-height: 24px;
    }

    .technology-cta .link {
      padding: 0 40px;
    }

    .btn-reservation {
      padding: 0 30px;
      height: 50px;
      line-height: 50px;
      font-size: 13px;
      letter-spacing: 1.2px;
    }

    .banner-trustindex {
      bottom: 30px;
      right: 40px;
      max-width: 250px;
    }

    .trustindex-widget {
      padding: 15px;
    }

    .trustindex-stars {
      font-size: 20px;
      margin-bottom: 6px;
    }

    .trustindex-rating {
      font-size: 24px;
    }

    .trustindex-text {
      font-size: 13px;
    }

    @keyframes banner_logo {
      0% { transform: translate(-50%, -50%); }
      88% { transform: translate(-50%, -50%); }
      100% { transform: translate(-50%, calc(-50% + 30px)); }
    }

    @keyframes banner_logo_black {
      0% { opacity: 0; }
      15% { opacity: 0; }
      55% { opacity: 1; }
      57% { opacity: 1; }
      63% { opacity: 1; }
      70% { opacity: 0; }
    }

    .home_bloc {
      padding: 0 0 100px 0;
      margin-top: 0;
    }

    .home_bloc .quote {
      display: none;
    }

    .home_bloc .bloc_shadow {
      width: 100%;
      margin-left: 0;
      transform: translateY(-60px);
    }

    .home_bloc .slider {
      width: 100%;
      position: relative;
      padding: 0;
      margin-bottom: 40px;
      display: block;
    }

    .home_bloc .slider .item {
      width: 100%;
      height: 300px;
    }

    .about-description-home p {
      font-size: 14px;
      line-height: 25px;
    }

    .home-stats {
      gap: 18px;
      margin: 25px 0;
    }

    .home-stat-number {
      font-size: 30px;
    }
  }

  @media (max-width: 600px) {
    .banner .video-container iframe {
      min-height: 80vh;
      min-width: 100vw;
    }

    .banner .logo {
      width: 240px;
    }

    .banner-cta {
      bottom: 20px;
      left: 5%;
      right: 5%;
    }

    .section-why-choose {
      padding: 50px 0;
    }

    .why-intro {
      font-size: 15px;
      line-height: 26px;
    }

    .why-grid {
      gap: 25px;
    }

    .why-item {
      padding: 25px 20px;
    }

    .why-icon svg {
      width: 40px;
      height: 40px;
    }

    .why-title {
      font-size: 18px;
    }

    .section-testimonials {
      padding: 50px 0 30px 0;
    }

    .testimonials-header {
      margin-bottom: 25px;
    }

    .testimonials-header .titre_main {
      font-size: 26px;
    }

    .testimonials-subtitle {
      font-size: 14px;
      line-height: 24px;
    }

    .testimonial-card {
      padding: 30px 20px;
    }

    .testimonial-badge {
      top: 15px;
      right: 15px;
      padding: 6px 12px;
      font-size: 10px;
    }

    .testimonial-header-with-photo {
      flex-direction: column;
      text-align: center;
      gap: 15px;
      margin-bottom: 20px;
    }

    .testimonial-photo {
      width: 70px;
      height: 70px;
    }

    .testimonial-stars {
      font-size: 18px;
      margin-bottom: 8px;
    }

    .testimonial-text {
      font-size: 13px;
      line-height: 24px;
    }

    .author-name {
      font-size: 15px;
    }

    .author-location {
      font-size: 11px;
    }

    .carousel-btn {
      width: 40px;
      height: 40px;
      font-size: 20px;
    }

    .prev-btn {
      left: 5px;
    }

    .next-btn {
      right: 5px;
    }

    .carousel-indicators {
      margin-top: 15px;
      gap: 8px;
    }

    /* Touch targets must remain 44px minimum for accessibility */

    .section-technology {
      padding: 50px 0;
    }

    .technology-header {
      margin-bottom: 35px;
    }

    .technology-grid {
      gap: 25px;
      margin-bottom: 35px;
    }

    .tech-image {
      height: 180px;
    }

    .tech-content {
      padding: 20px;
    }

    .tech-title {
      font-size: 18px;
    }

    .tech-subtitle {
      font-size: 13px;
      margin-bottom: 12px;
    }

    .tech-description {
      font-size: 13px;
      line-height: 22px;
    }

    .technology-cta .link {
      width: 100%;
      padding: 0 20px;
    }

    .btn-reservation {
      width: 100%;
      padding: 0 20px;
      height: 48px;
      line-height: 48px;
      font-size: 12px;
      letter-spacing: 1px;
    }

    .banner-trustindex {
      display: none;
    }

    .home_bloc .bloc_shadow .link {
      width: 100%;
    }

    .about-description-home p {
      font-size: 14px;
      line-height: 24px;
      margin-bottom: 12px;
    }

    .home-stats {
      grid-template-columns: repeat(3, 1fr);
      gap: 15px;
      margin: 20px 0;
    }

    .home-stat-number {
      font-size: 28px;
    }

    .home-stat-label {
      font-size: 11px;
    }

    :global(body.sticky) .home_bloc .bloc_shadow {
      transform: none;
    }
  }

  /* Popup Promo */
  .promo-popup-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(5px);
    z-index: 9999;
    display: none;
    align-items: center;
    justify-content: center;
    animation: fadeIn 400ms ease-in-out;
  }

  .promo-popup-overlay.show {
    display: flex;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes popupSlideIn {
    from {
      transform: translateY(-50px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  .promo-popup {
    background: #fff;
    border-radius: 20px;
    max-width: 600px;
    width: 90%;
    padding: 50px 40px;
    position: relative;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    text-align: center;
    animation: popupSlideIn 500ms ease-out;
    overflow: hidden;
  }

  .promo-popup::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 8px;
    background: linear-gradient(135deg, #bfa477 0%, #d2bb94 100%);
  }

  .popup-close {
    position: absolute;
    top: 15px;
    right: 15px;
    width: 40px;
    height: 40px;
    border: none;
    background: #f5f5f5;
    border-radius: 50%;
    font-size: 28px;
    line-height: 1;
    color: #666;
    cursor: pointer;
    transition: all 300ms ease;
    z-index: 10;
  }

  .popup-close:hover {
    background: #bfa477;
    color: #fff;
    transform: rotate(90deg);
  }

  .popup-badge {
    display: inline-block;
    background: linear-gradient(135deg, #bfa477 0%, #d2bb94 100%);
    color: #fff;
    padding: 8px 20px;
    border-radius: 25px;
    font-family: "Barlow", sans-serif;
    font-size: 13px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 25px;
  }

  .popup-title {
    font-family: "PT Serif", serif;
    font-size: 36px;
    font-weight: 700;
    color: #222;
    margin-bottom: 15px;
    line-height: 1.2;
  }

  .popup-subtitle {
    font-size: 16px;
    color: #666;
    margin-bottom: 35px;
    line-height: 1.6;
  }

  .popup-highlight {
    display: flex;
    justify-content: center;
    gap: 40px;
    margin-bottom: 35px;
    padding: 30px 0;
    background: linear-gradient(135deg, rgba(191, 164, 119, 0.1) 0%, rgba(210, 187, 148, 0.1) 100%);
    border-radius: 15px;
  }

  .highlight-item {
    text-align: center;
  }

  .highlight-number {
    font-family: "PT Serif", serif;
    font-size: 48px;
    font-weight: 700;
    color: #bfa477;
    line-height: 1;
    margin-bottom: 8px;
  }

  .highlight-label {
    font-family: "Barlow", sans-serif;
    font-size: 14px;
    color: #666;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .popup-description {
    font-size: 15px;
    color: #444;
    line-height: 1.7;
    margin-bottom: 30px;
  }

  .popup-cta {
    display: inline-block;
    padding: 18px 50px;
    background: linear-gradient(135deg, #bfa477 0%, #d2bb94 100%);
    color: #fff;
    text-decoration: none;
    font-family: "Barlow", sans-serif;
    font-size: 16px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1.2px;
    border-radius: 50px;
    box-shadow: 0 10px 30px rgba(191, 164, 119, 0.3);
    transition: all 400ms ease;
    margin-bottom: 20px;
  }

  .popup-cta:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 40px rgba(191, 164, 119, 0.5);
  }

  .popup-terms {
    font-size: 12px;
    color: #999;
    margin: 0;
  }

  @media (max-width: 600px) {
    .promo-popup {
      padding: 40px 25px;
    }

    .popup-title {
      font-size: 28px;
    }

    .popup-highlight {
      gap: 20px;
      padding: 20px;
    }

    .highlight-number {
      font-size: 36px;
    }

    .popup-cta {
      padding: 15px 40px;
      font-size: 14px;
    }
  }

  /* Headband - texte en blanc pour meilleure visibilité */
  .headband {
    color: #fff;
  }

  .headband strong {
    color: #fff;
    font-weight: 700;
  }

  .headband.mood {
    color: #fff;
  }

  .headband.mood strong {
    color: #fff;
    font-weight: 700;
  }

  /* Section Traitement - Image plus petite */
  .section-treatment .photo_texte .photo img {
    max-width: 80%;
    height: auto;
    margin: 0 auto;
    display: block;
  }

  @media (max-width: 1200px) {
    .section-treatment .photo_texte .photo img {
      max-width: 100%;
    }
  }
</style>

<script>
  // Animation d'ouverture au chargement - Semeraro Style
  document.addEventListener('DOMContentLoaded', () => {
    const wrapper = document.querySelector('.page-wrapper');

    // Retirer start-anim au scroll et ajouter sticky
    window.addEventListener('scroll', () => {
      const posScroll = window.pageYOffset;
      if (posScroll > 0) {
        wrapper?.classList.remove('start-anim');
      }

      // Ajouter sticky class quand on scroll (Semeraro behavior)
      if (posScroll > 200) {
        document.body.classList.add('sticky');
      } else {
        document.body.classList.remove('sticky');
      }
    });

    // Activer les animations au scroll
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -100px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('play');
        }
      });
    }, observerOptions);

    // Observer tous les éléments avec la classe animate
    const animateElements = document.querySelectorAll('.animate');
    animateElements.forEach(el => observer.observe(el));

    // Testimonials Carousel
    const carouselTrack = document.querySelector('.carousel-track');
    const testimonialCards = document.querySelectorAll('.testimonial-card');
    const prevBtn = document.querySelector('.prev-btn');
    const nextBtn = document.querySelector('.next-btn');
    const indicators = document.querySelectorAll('.indicator');

    let currentIndex = 0;
    const totalSlides = testimonialCards.length;

    function updateCarousel(index) {
      // Update track position
      const offset = -index * 100;
      carouselTrack.style.transform = `translateX(${offset}%)`;

      // Update active card
      testimonialCards.forEach((card, i) => {
        card.classList.toggle('active', i === index);
      });

      // Update active indicator
      indicators.forEach((indicator, i) => {
        indicator.classList.toggle('active', i === index);
      });

      currentIndex = index;
    }

    // Previous button
    prevBtn?.addEventListener('click', () => {
      const newIndex = currentIndex === 0 ? totalSlides - 1 : currentIndex - 1;
      updateCarousel(newIndex);
    });

    // Next button
    nextBtn?.addEventListener('click', () => {
      const newIndex = currentIndex === totalSlides - 1 ? 0 : currentIndex + 1;
      updateCarousel(newIndex);
    });

    // Indicator buttons
    indicators.forEach((indicator, index) => {
      indicator.addEventListener('click', () => {
        updateCarousel(index);
      });
    });

    // Auto-play carousel (optional - every 5 seconds)
    let autoplayInterval = setInterval(() => {
      const newIndex = currentIndex === totalSlides - 1 ? 0 : currentIndex + 1;
      updateCarousel(newIndex);
    }, 5000);

    // Pause autoplay on hover
    const carouselContainer = document.querySelector('.testimonials-carousel');
    carouselContainer?.addEventListener('mouseenter', () => {
      clearInterval(autoplayInterval);
    });

    // Resume autoplay on mouse leave
    carouselContainer?.addEventListener('mouseleave', () => {
      autoplayInterval = setInterval(() => {
        const newIndex = currentIndex === totalSlides - 1 ? 0 : currentIndex + 1;
        updateCarousel(newIndex);
      }, 5000);
    });

    // Touch/swipe support for mobile
    let touchStartX = 0;
    let touchEndX = 0;

    carouselContainer?.addEventListener('touchstart', (e) => {
      touchStartX = e.changedTouches[0].screenX;
    });

    carouselContainer?.addEventListener('touchend', (e) => {
      touchEndX = e.changedTouches[0].screenX;
      handleSwipe();
    });

    function handleSwipe() {
      const swipeThreshold = 50;
      const diff = touchStartX - touchEndX;

      if (Math.abs(diff) > swipeThreshold) {
        if (diff > 0) {
          // Swipe left - next
          const newIndex = currentIndex === totalSlides - 1 ? 0 : currentIndex + 1;
          updateCarousel(newIndex);
        } else {
          // Swipe right - prev
          const newIndex = currentIndex === 0 ? totalSlides - 1 : currentIndex - 1;
          updateCarousel(newIndex);
        }
      }
    }

    // Popup Promo
    const promoPopup = document.getElementById('promo-popup');
    const popupClose = document.getElementById('popup-close');

    // Afficher le popup après 3 secondes
    setTimeout(() => {
      // Vérifier si le popup a déjà été affiché dans cette session
      if (!sessionStorage.getItem('promoPopupShown')) {
        promoPopup?.classList.add('show');
        sessionStorage.setItem('promoPopupShown', 'true');
      }
    }, 3000);

    // Fermer le popup
    popupClose?.addEventListener('click', () => {
      promoPopup?.classList.remove('show');
    });

    // Fermer en cliquant sur l'overlay
    promoPopup?.addEventListener('click', (e) => {
      if (e.target === promoPopup) {
        promoPopup.classList.remove('show');
      }
    });

    // Fermer avec la touche Échap
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && promoPopup?.classList.contains('show')) {
        promoPopup.classList.remove('show');
      }
    });

    // Technology Carousel (mobile/tablet only)
    const techGrid = document.querySelector('.technology-grid');
    const techPrevBtn = document.querySelector('.tech-prev-btn');
    const techNextBtn = document.querySelector('.tech-next-btn');
    const techIndicators = document.querySelectorAll('.tech-indicator');
    const techItems = document.querySelectorAll('.tech-item');

    if (techGrid && techPrevBtn && techNextBtn && techIndicators.length > 0) {
      let techCurrentIndex = 0;
      const techTotalSlides = techItems.length;

      function updateTechCarousel(index) {
        // Update grid position using viewport width
        const containerWidth = techGrid.parentElement.offsetWidth;
        const offset = -index * containerWidth;
        techGrid.style.transform = `translateX(${offset}px)`;

        // Update active indicator
        techIndicators.forEach((indicator, i) => {
          indicator.classList.toggle('active', i === index);
        });

        techCurrentIndex = index;
      }

      // Previous button
      techPrevBtn.addEventListener('click', () => {
        const newIndex = techCurrentIndex === 0 ? techTotalSlides - 1 : techCurrentIndex - 1;
        updateTechCarousel(newIndex);
      });

      // Next button
      techNextBtn.addEventListener('click', () => {
        const newIndex = techCurrentIndex === techTotalSlides - 1 ? 0 : techCurrentIndex + 1;
        updateTechCarousel(newIndex);
      });

      // Indicators
      techIndicators.forEach((indicator, index) => {
        indicator.addEventListener('click', () => {
          updateTechCarousel(index);
        });
      });

      // Touch support
      let techTouchStartX = 0;
      let techTouchEndX = 0;

      techGrid.addEventListener('touchstart', (e) => {
        techTouchStartX = e.changedTouches[0].screenX;
      });

      techGrid.addEventListener('touchend', (e) => {
        techTouchEndX = e.changedTouches[0].screenX;
        handleTechSwipe();
      });

      function handleTechSwipe() {
        const swipeThreshold = 50;
        const diff = techTouchStartX - techTouchEndX;

        if (Math.abs(diff) > swipeThreshold) {
          if (diff > 0) {
            // Swipe left - next
            const newIndex = techCurrentIndex === techTotalSlides - 1 ? 0 : techCurrentIndex + 1;
            updateTechCarousel(newIndex);
          } else {
            // Swipe right - prev
            const newIndex = techCurrentIndex === 0 ? techTotalSlides - 1 : techCurrentIndex - 1;
            updateTechCarousel(newIndex);
          }
        }
      }
    }
  });
</script>
